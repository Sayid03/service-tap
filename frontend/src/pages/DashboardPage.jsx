export default function DashboardPage() {
    return (
      <section>
        <h1>Dashboard</h1>
        <p>You are logged in. Next we will connect this page to /users/me/ and show role-based actions.</p>
      </section>
    );
  }