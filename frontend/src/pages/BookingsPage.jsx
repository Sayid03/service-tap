export default function BookingsPage() {
    return (
      <section>
        <h1>My bookings</h1>
        <p>This page will be connected after auth bootstrap is added.</p>
      </section>
    );
  }