export default function ServiceDetailPage() {
    return (
      <section>
        <h1>Service detail</h1>
        <p>This page will be wired after the backend detail route is fixed.</p>
      </section>
    );
  }